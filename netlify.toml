[build]
  command = "python -m pip install -r requirements.txt && mkdocs build"
  publish = "site"

[build.environment]
  PYTHON_VERSION = "3.11"
  PIP = "python -m pip"
  PATH = "/opt/build/repo/bin:${PATH}"
  NETLIFY_SKIP_INSTALL = "true"

# Redirect legacy Lab URLs to Insights
[[redirects]]
  from = "/lab/*"
  to = "/insights/:splat"
  status = 301
  force = true

[[headers]]
  for = "/work/personal-finance/*"
  [headers.values]
    Basic-Auth = "pf:changeme"
